% ============================================================
% audit_table.tex
%
% A "system audit" table mapping the paper's claims to the evidence
% backing each one.  This is *the* canonical source of truth for which
% claims are PASS / PART / STUB / FAIL --- prose elsewhere in the paper
% should not contradict it.
%
% Status semantics (paste into the abstract or front-matter as well):
%   PASS  -- derivation complete or experiment confirms the claim to
%            stated precision.
%   PART  -- mechanism demonstrated, full quantitative match pending;
%            specific gaps documented in the evidence column.
%   STUB  -- analytical result with no numerical confirmation yet,
%            or planned experiment not yet run.
%   FAIL  -- tested and disconfirmed.
%
% Use \texttt{} for code/experiment names and file paths inside cells.
% Long cells are fine -- the column widths are tuned for prose.
% ============================================================

\label{tab:audit}

{\scriptsize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{0.95}
\begin{longtable}{@{}p{2.6cm}p{3.8cm}p{3.2cm}p{4.4cm}p{1.0cm}@{}}
\caption{\small System audit: paper claims mapped to supporting
derivations and experiments.  Status semantics in the front-matter
(\texttt{PASS} / \texttt{PART} / \texttt{STUB} / \texttt{FAIL}).}
\label{tab:audit_caption} \\
\hline
\textbf{Claim} & \textbf{Mechanism} & \textbf{Continuum / formal limit} & \textbf{Evidence} & \textbf{Status} \\
\hline
\endfirsthead

\multicolumn{5}{l}{\textit{(Table~\ref{tab:audit}, continued from previous page)}} \\
\hline
\textbf{Claim} & \textbf{Mechanism} & \textbf{Continuum / formal limit} & \textbf{Evidence} & \textbf{Status} \\
\hline
\endhead

\hline
\multicolumn{5}{r}{\textit{(continued on next page)}} \\
\endfoot

\hline
\endlastfoot

% ── Kinematic channel ───────────────────────────────────────────────
Kinematic directional dispersion anisotropy along $(1,1,-1)$ (axial--transverse contrast)
    & Uniaxial axial--transverse anisotropy of the structure-factor magnitude: $M_\text{eff} = \tfrac43(\mathbb{I}-\hat{\mathbf{n}}_*\hat{\mathbf{n}}_*^T) = \tfrac43 P_\perp$, so $|H_\text{RGB}(\mathbf{k})|^2 = 1-\tfrac43|\mathbf{k}_\perp|^2$, flat along $(1,1,-1)$. Massive-sector group speed $\tfrac23\sin\omega\,|\mathbf{k}|\sin\alpha$ -- a contrast between propagation \emph{directions}, not a fixed-$\mathbf{k}$ polarization split (hence \emph{not} birefringence)
    & Isotropic under $O_h$ averaging / $a\to 0$
    & \emph{Analytic:} $|H_\text{RGB}|^2 = 1$ exactly along $(1,1,-1)$ at all orders; \texttt{exp\_02\_closed\_form\_split\_check} (identities $<10^{-12}$, speed coeffs $<10^{-5}$). \emph{Observability:} \texttt{exp\_01\_optical\_axis\_isotropy} -- isotropic pulse flattens perpendicular to $(1,1,-1)$, survives exact A=1 renormalization. \emph{Interpretation (what PASS covers):} the eigenphase-$\to$-group-velocity reading is \emph{derived} for the massive sector ($\omega\neq0$) -- the A=1 per-tick renormalization preserves phase for a pure eigenmode (it removes only the scalar attenuation, so the eigenphase is the energy), while for a general state it is a nonlinear global-norm rescale, \emph{not} a unitary operator -- the group-velocity reading is then justified via stationary phase for a narrow-band packet (centroid velocity $\nabla_{\mathbf{k}}\theta$; Sec.~\ref{subsec:a1_normalization}). At $\omega=0$ the eigenphase is \emph{exactly flat} (Prop.~\ref{prop:massless_flat}: $\max|\nabla_{\mathbf{k}}\theta_+|=0$): the massless-limit anisotropy sits in the eigenvalue \emph{magnitude} only, so there is no photon energy dispersion and the massless-spinor-$\to$-photon identification is not made. The directional dispersion is thus a \emph{massive-sector} effect; the photon-sector dispersion is the gauge common mode ($\omega^2=k^T\boldsymbol\varepsilon k$, its own rows).
    & \texttt{PASS} \\

% ── Gauge sector: null polarization split (conditional on the blocks) ─
Gauge-sector null polarization split (birefringence cancels), \emph{conditional on the geometric blocks}
    & With $\boldsymbol\mu^{-1} = \gamma\operatorname{adj}(\boldsymbol\varepsilon)$ the photon dispersion is a \emph{double} transverse root $\det(K\boldsymbol\mu^{-1}K+\omega^2\boldsymbol\varepsilon) = \det(\boldsymbol\varepsilon)\,\omega^2(\omega^2-\gamma\,k^T\boldsymbol\varepsilon k)^2$ for every direction, so the split $\propto|\varepsilon_a\mu^{-1}_a - \varepsilon_p\mu^{-1}_p| = 0$
    & Isotropic-impedance product $\varepsilon_i(\mu^{-1})_i = \gamma\det\boldsymbol\varepsilon$; rescaling-invariant, so immune to the deferred normalization; holds for any hop vectors
    & \emph{Adjugate theorem} $Q_B = \operatorname{adj}(\boldsymbol\varepsilon)$, $\boldsymbol\varepsilon = MM^T$, proven for general vectors (\texttt{electric\_induced\_action.py}, symbolic). \emph{Engine + numeric:} \texttt{exp\_03\_geometric\_blocks} confirms the adjugate from the two engine blocks ($5\times10^{-15}$); the Fresnel solver \texttt{exp\_03\_dispersion\_core} gives $\max|v_+-v_-| = 1.6\times10^{-15}$ over 2000 directions (detuned control $0.5$); $2\times10^4$-$k$ symbolic-guard sweep $<10^{-14}$. An \emph{independent code-path verification} (same framework/engine), not a separate physical measurement.
    & \texttt{PASS} \\

% ── Gauge sector: blocks as the dynamical induced action ─────────────
Gauge blocks as the actual \emph{dynamical} induced action ($\boldsymbol\varepsilon$, $\boldsymbol\mu^{-1}$)
    & The two forms are the natural \emph{geometric} plaquette quadratic forms: $\boldsymbol\varepsilon = \sum_a\mathbf{V}_a\mathbf{V}_a^T$ (temporal holonomy $\Theta_a = \mathbf{V}_a\!\cdot\!\mathbf{E}$, eig $\{1,4,4\}$), $\boldsymbol\mu^{-1} = Q_B$ (spatial holonomy, eig $\{4,4,16\}$)
    & The dynamical induced action is a matter-integrated (loop-level) effective action; the plaquette forms are bare geometric actions
    & \emph{Vertex verified:} $\delta\phi = \omega+V$ read off \texttt{HopOperator.step} (\texttt{exp\_04\_electric\_permittivity\_extraction}; fidelity $4\times10^{-19}$, $\omega$ cancels, $a_t=1$). \emph{Gap (why PART):} this validates the field--matter \emph{vertex}, not the quadratic effective action $\Gamma^{(2)}$; a general $D_{3d}$-invariant form has two free coefficients, so the $1{:}4$ ratio is not symmetry-forced. Promotion needs $\varepsilon_{ij}\propto\partial^2\Gamma/\partial E_i\partial E_j$ ($\Gamma=-\log\det T$) or a measured 2nd-order response; the $\operatorname{Tr}\ln T$ route is Ward-divergent on the flat axis (naive band sum $\to$ wrong object about $(1,-1,0)$) and is a stated open follow-on.
    & \texttt{PART} \\

% ── Gauge sector: common-mode speed anisotropy (a stated tension) ───
Gauge-sector common-mode speed anisotropy (factor $\sim2$ about $(1,1,-1)$) -- a stated \emph{tension}
    & Both polarizations share $v^2(\hat{\mathbf{k}}) = \gamma\,\hat{\mathbf{k}}^T\boldsymbol\varepsilon\,\hat{\mathbf{k}} \in [\gamma,4\gamma]$; the \emph{ratio} $v^2_{\max}/v^2_{\min}=4$ (factor-$\sim2$ speed) is independent of the undetermined normalization $\gamma$. $\boldsymbol\varepsilon = \sum_a\mathbf{V}_a\mathbf{V}_a^T = \{1,4,4\}$ is $O(1)$, dimension-4, and (three routes: geometric, Ward-safe band-sum, density response) \emph{not} suppressed. The matter shares no such leading anisotropy (the matter's is dim-6), so the tilt is observable
    & Isotropy would need the cubic group: $\sum_a\mathbf{V}_a\mathbf{V}_a^T = 4\mathbb{I}$ for the \emph{four} body-diagonals. The anisotropy is exactly the omission of the 4th axis (single trigonal domain); a four-orientation vacuum would restore $O_h$ for the induced gauge loop while matter keeps its dim-6 axis
    & As constructed (single domain), an $O(1)$ direction-dependent speed of light is a marginal CPT-even $k_F$ Lorentz violation \emph{excluded} by cavity/Michelson--Morley isotropy bounds ($\sim10^{-18}$). \emph{Decisive} for the geometric completion; \emph{strong-provisional} for the dynamical model (the vertex is verified, the full $\Gamma^{(2)}$ Hessian open), removed once a nonzero dim-4 traceless part is confirmed -- any unsuppressed anisotropy is fatal. \texttt{exp\_03\_geometric\_blocks} + \texttt{notes/gauge\_common\_mode\_order.md}. \emph{Structural resolution} (four-axis / higher-dimensional / polygonal hop topologies the formalism admits) is a stated open foundational item; does not touch the birefringence null or the kinematic channel.
    & \texttt{FAIL} \emph{(geometric: decisive; dynamical: strong-provisional; open under $O_h$)} \\

% ── Concordance ─────────────────────────────────────────────────────
Multi-channel concordance (P9)
    & Kinematic and gauge sectors inherit the \emph{same} optical axis $(1,1,-1)$ from the common bipartite geometry ($M_\text{eff}$, $\boldsymbol\varepsilon$, $\boldsymbol\mu^{-1}$ special directions coincide)
    & N/A (cross-sector structural identity)
    & Axis-sharing is a \emph{structural} identity (derived): all three tensors inherit $(1,1,-1)$ from the same geometry. The gauge leading birefringence \emph{cancels}, so the shared axis registers there as the direction of the common-mode speed anisotropy, not a polarization split. A genuine structural concordance; its strength as a multi-\emph{observable} claim is limited because the gauge birefringence is null, the massive-sector dispersion's astrophysical reach is Planck-suppressed, and the common-mode speed is a tension (excluded as constructed) rather than a clean prediction (see the falsifiability row).
    & \texttt{PART} \\

% ── Falsifiability map (multi-channel) ──────────────────────────────
Falsifiability of the framework along the optical axis (multi-channel map)
    & The forbidden optical-axis \emph{birefringence} is itself a \emph{passed} falsification test (a postdiction: the bounds predate the framework): the framework forbids it (Theorem~\ref{thm:doubled_root}), the quantity is tightly bounded and unobserved, and the same datum \emph{excludes} the CPT-odd, dimension-five and other birefringent Lorentz-violating rivals while corroborating the framework's CPT-even, non-birefringent class. Forward, the testable content is the dispersion, which splits by field
    & N/A (scope statement)
    & Multi-channel map (Sec.~\ref{sec:falsifiability}, Table there): (i) \emph{birefringence} = forbidden, \emph{passed} (corroborated, excludes rivals; \emph{conditional} on the constitutive blocks); (ii) \emph{kinematic massive dispersion} = candidate falsifiable, dim-6, real but Planck-suppressed, distinctive \emph{sidereal} signature (\texttt{PART}); (iii) \emph{$\omega=0$ magnitude/filtering} anisotropy = real observable, testability open (\texttt{PART}); (iv) \emph{gauge-sector photon} common-mode speed = a stated \emph{tension} -- an $O(1)$, dim-4 anisotropy that, for the single-domain construction, is a Lorentz violation \emph{excluded} by isotropy bounds; the structural fix (four-axis / $O_h$-restoring hop topology) is an open foundational item (own row). Honesty guardrails: the passed null no longer \emph{discriminates} the framework prospectively, and it is corroborated-\emph{and}-conditional. SME placement per Kosteleck\'y--Mewes.
    & \texttt{PART} \\

\hline
\end{longtable}
}
