# v1.0 -- Optical-axis birefringence cancellation, and a single-domain no-go

The first public release of Paper IV of the A=1 Discrete Causal Lattice series.
The lattice singles out an optical axis $\hat{\mathbf{n}}_* = (1,1,-1)/\sqrt3$ —
the one cube body-diagonal absent from its three-vector hop set — and this single
geometric fact organizes both its matter and its gauge sector. The paper reports a
clean survivor beside a clean no-go: a birefringence cancellation that holds for
any three-hop lattice (given the geometric blocks), and a single-domain
construction that is Lorentz-violating in **both** the induced-photon and the
bare-matter sectors, and so is excluded as constructed. This release subsumes the
former companion study (Paper VIII, the electric induced-action block), merged
here because the shared optical axis is the natural unity of the two effects.

## Why this is v1.0

v1.0 establishes a citable reference for the rest of the series. It closes the
gauge-sector verdict (birefringence cancels; the common-mode speed is a stated
tension), the matter-sector as-constructed exclusion (verified from the paper's
own transfer operator), and incorporates a full external referee pass. Open
foundational items are documented as explicit follow-on scope rather than hidden.

## Headline results

### Audit-table status

- **Gauge-sector birefringence null** (`exp_03_geometric_blocks`,
  `exp_03_dispersion_core`, `PASS`) — the adjugate closure
  $\boldsymbol{\mu}^{-1} = \gamma\operatorname{adj}(\boldsymbol{\varepsilon})$
  forces a doubled transverse root; the split cancels to $10^{-15}$.
- **Kinematic directional dispersion anisotropy** (`exp_02`, `PASS` — substrate).
- **Bare matter as literal lab matter** (`exp_05`, `FAIL` as-constructed) — an
  exact flat band, $O(1)$ kinetic anisotropy, and a linearly-accumulating filter;
  open via the emergence map.
- **Gauge common-mode speed anisotropy** (`FAIL` as-constructed) — $O(1)$,
  dimension-four, excluded by isotropy bounds for the single domain.

### Paper-section highlights

- **§2** the closed-form kinematics and the as-constructed matter exclusion.
- **§3** the adjugate theorem, the doubled-root cancellation (a lattice instance
  of the classical impedance-matched medium), and the common-mode tension.
- **§4** a per-channel falsifiability no-go map and its SME placement.
- **Appendix C** the Ward-safe band-sum estimate behind the dynamical verdict.

## Reproducibility

From a fresh clone of the `v1.0` tag: `make paper` builds the PDF (~30 s). The
headline numerical claims each run in seconds:
`python src/experiments/exp_02_closed_form_split_check.py`,
`exp_05_matter_sector_order.py`, and `exp_03_geometric_blocks.py` +
`exp_03_dispersion_core.py`. See Appendices A and B.

## What is not in v1.0

The token→matter emergence map, the single-object dynamical
$\operatorname{Tr}\ln T$ tensor, and the $O_h$-restoring four-orientation vacuum
are stated open (they would lift the matter FAIL, the gauge PART, and the
common-mode tension respectively). The nonlinear-QM / signaling analysis of the
A=1 renormalization is deferred with candidate resolutions. See the audit table
and `release_notes/v1.0.md` for the full follow-on scope.

---

**Citation.** See `CITATION.cff`. Zenodo DOI for this release:
[10.5281/zenodo.21435952](https://doi.org/10.5281/zenodo.21435952).
